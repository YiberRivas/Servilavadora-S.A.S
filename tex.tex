git add .
git commit -m "Descripción de los cambios"
git push


descargar cambios 

git pull  
o 
git pull origin main




credenciales 

Super Admin	     admin@servilavadora.co	        123456	
Admin Empresa	 adminempresa@cleanhouse.co   	123456

#####################################################################
React Web (Vite)
npm run dev

React Native con Expo (Aplicación móvil)
npx expo start


bakend

cd Backend
python run.py
# o
python -m uvicorn app.main:app --host 127.0.0 --port 8000

python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

BACKEND:
para cambiar de ip en el archivo env.js de la carpeta config,
cambiar la ip de la variable API_BASE_URL y WS_BASE_URL, luego reiniciar el servidor de backend y el servidor de frontend.

FRONTEND:
para cambiar de ip en el archivo app.json, cambiar la ip de la variable apiBaseUrl y wsBaseUrl, luego reiniciar el servidor de frontend.